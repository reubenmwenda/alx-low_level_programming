#include "main.h"

/**
 * mul - multiplies two integers
 * @a: I mean it does something
 * @b: I mean it does something
 *
 * Return: result is returned
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
