#include"main.h"


/**
 * swap_int - checks long of string
 * @a: it does something
 * @b: it does something
 * Return: returns value of string
 */

void swap_int(int *a, int *b)
{

	int temp;

	temp = *a;
	*a = *b;
	*b = temp;
}
