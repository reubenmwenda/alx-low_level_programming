#include "main.h"
/**
 * _isalpha - Entry point
 * @c: I mean it does something
 * Description: checks for small letters and large
 *
 * Return: 1 if small or large and 0 if not small
 */

int _isalpha(int c)
{
	if ((c >= 97 && c <= 122) || (c >= 65 && c <= 90)			
	{
		return (1);

	}
	else
	{
		return (0);
	}
}
