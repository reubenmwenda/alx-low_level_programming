#include <stdio.h>

/**
 * main - prints numbers
 *
 * Return: returns 0 (Success)
 */
int main(void)
{

	/* local variable definition */
	int num = 0;

	while (num < 10)
	{
		printf("%d", num);

		num++;

	}
	printf("\n");

	return (0);
}
